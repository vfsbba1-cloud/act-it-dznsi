services:
  - type: web
    name: vfs-license-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT
    envVars:
      - key: ADMIN_USER
        value: admin
      - key: ADMIN_PASS
        value: admin123
